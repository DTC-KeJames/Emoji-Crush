import { EmojiType } from './types';

export const BOARD_SIZE = 8;
export const GAME_DURATION = 60; // seconds

export const EMOJIS: EmojiType[] = ['🍎', '🍇', '🍊', '🍋', '🫐', '🥥'];

// Points
export const POINTS_PER_GEM = 10;
export const POINTS_COMBO_BONUS = 50;
export const POINTS_PER_SECOND_LEFT = 5;

// Animation delays (ms)
export const ANIMATION_DURATION_SWAP = 0.3;
export const ANIMATION_DURATION_DROP = 0.4;